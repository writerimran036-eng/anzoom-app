name: anzom_app
description: AnZoom Flutter app with Firebase storage
publish_to: 'none' # private project

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.27.0
  firebase_storage: ^11.3.0
  image_picker: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true